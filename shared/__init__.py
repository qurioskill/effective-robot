"""Shared helpers used by every agent example.

Keeping configuration and the LLM factory in one place means the instructor can
change the model (or swap the API key handling) once and have it apply to all
five examples.
"""
